{
  "name": "srclib-nomos",
  "version": "1.0.0",
  "description": "Srclib toolchain that wraps the nomos license scanner.",
  "main": ".bin/srclib-nomos",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPL-2.0",
  "dependencies": {
    "fs-walk": "0.0.1",
    "gitignore-parser": "0.0.1",
    "nomnom": "^1.8.1"
  }
}
